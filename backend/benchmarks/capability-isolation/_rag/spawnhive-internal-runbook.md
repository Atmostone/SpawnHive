# SpawnHive Internal Runbook (fictional — benchmark fixture)

This document contains intentionally fictional internal facts used by the
capability-isolation `private_data` benchmark cases. None of it is real; it exists
only so an agent must retrieve it via the knowledge base rather than recall it.

- The code name of the **Q3 2026 release** is **Project Halcyon**.
- The **staging Qdrant** instance listens on TCP port **6344**.
- The **on-call owner of the billing service** is **Marina Velez**.
- **Incident postmortems** are retained for **540 days**.
- The **default per-agent RAM cap** is **768 MB**.
- The **canary cohort** receives **7%** of traffic.
- **Provider API keys** are rotated every **45 days**.
- The **internal status page** slug is **aurora-status**.
