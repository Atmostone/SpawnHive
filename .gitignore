# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Node
node_modules/
dist/
.vite/

# Environment
.env
.env.local
.env.prod

# Secrets / deploy access — NEVER commit (repo is public)
.deploy_info
.keys

# Data (persistent volumes — runtime artifacts, never committed)
data/workspaces/
data/shared/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker / logs
*.log

# Local-only compose override (per-machine port remap)
docker-compose.override.yml

# Private material — never in the public repo
.claude/
research/
reports/
final_reports/
# research_old/ = the archived research/ + reports/ + final_reports/ trees (renamed 2026-08),
# research_2/ = the current working set. Both are private. NOTE: renaming a private
# directory silently drops it out of these rules — add the new name here in the same commit.
research_old/
research_2/
# Submitted deliverables — private, kept out of the public repo
Описание проекта.pdf
*.pptx
